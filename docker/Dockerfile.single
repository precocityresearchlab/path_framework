FROM python:3.11-slim

WORKDIR /app

# Install uv
RUN pip install uv

# Copy project files
COPY pyproject.toml uv.lock ./

# Install dependencies with uv
RUN uv sync --frozen

# Copy source code
COPY src/ ./src/

# Set environment variables
ENV DEPLOYMENT_MODE=single_container
ENV AGENT_PROFILES=all

# Expose port
EXPOSE 8000

# Run all agents in single process
CMD ["uv", "run", "python", "-m", "src.main"]